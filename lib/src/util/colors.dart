import 'package:flutter/material.dart';

abstract class CardColors {
    static const red = Color(0xFFDC3545);
    static const green = Color(0xFF28A745);
    static const gray = Color(0xFF6C757D);
    static const blue = Color(0xFF169DB2);
}